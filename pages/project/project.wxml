<view class="{{search}}">
	<view class="weui-flex" wx:if="{{projectListShow}}">

		<view class="weui-flex__item">
			<picker bindchange="bindBuildChange" value="{{buildIndex}}" range="{{buildArray}}">
				<view class="search-type">
					<image src="/images/sgdw.png"></image>
					<text>{{buildArray[buildIndex]}}</text>
				</view>
			</picker>
		</view>

		<!-- 画竖线 -->
		<view class="middle"></view>

		<view class="weui-flex__item">
			<picker bindchange="bindCategoryChange" value="{{categoryIndex}}" range="{{categoryArray}}">
				<view class="search-type">
					<image src="/images/xmlb.png"></image>
					<text>{{categoryArray[categoryIndex]}}</text>
				</view>
			</picker>
		</view>
	</view>
	<view wx:if="{{projectInfoShow}}">
		<button class="projectBack" bindtap="showProjectList">返回</button>
	</view>
</view>

<view class="page project-list-info" data-weui-theme="{{theme}}">

	<!-- 项目列表页 -->
	<view class="page__bd" wx:if="{{projectListShow}}">

		<view class="weui-cells__title">项目名称</view>
		<view class="weui-cells weui-cells_after-title">

			<view data-text="{{item.projectName}}" data-id="{{item.id}}" data-categoryType="{{item.categoryType}}" bindtap="showProjectInfo" class="weui-cell weui-cell_example " wx:for="{{projectList}}" wx:key="index" hover-class="weui-cell_active">
				<view class="weui-cell__bd">{{item.projectName}}</view>
				<!-- <view class="weui-cell__ft">{{item.buildType}}</view> -->
			</view>
		</view>

	</view>

	<!-- 项目详情页 -->
	<block wx:if="{{projectInfoShow}}">
		<!-- <view class="page__bd"> -->
		<!-- 项目名称区域 -->
		<view style="text-align: center">
			<text>{{projectName}}</text>
		</view>

		<!-- 图片区域 -->
		<view id="projectImage">
			<block wx:if="{{projectInfo.effectPicture != ''}}">
				<image bindtap="toTableData" src="{{imagePath}}" style="width: 100%"></image>
			</block>
			<block wx:if="{{projectInfo.effectPicture == ''}}">
				<view bindtap="toTableData">未设置图片</view>
			</block>
		</view>

		<!-- 项目信息区域 -->
		<!-- ==================基本信息start================== -->
		<!--房建类start-->
		<block wx:if="{{categoryType == 1}}">
			<view class="table">
				<view class="tr bg-w">
					<view class="th jbxx" bindtap="toJbxx">基本信息</view>
				</view>

				<view class="tr">
					<view class="td bg-g">占地面积</view>
					<view class="td">{{projectInfo.coverArea}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">总建筑面积</view>
					<view class="td">{{projectInfo.totalArea}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">楼栋数</view>
					<view class="td">{{projectInfo.buildNum}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">住宅套数</view>
					<view class="td">{{projectInfo.houseNum}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">店面间数</view>
					<view class="td">{{projectInfo.storeNum}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">商业面积</view>
					<view class="td">{{projectInfo.businessArea}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">住宅面积</view>
					<view class="td">{{projectInfo.houseArea}}</view>
				</view>

				<!--相同的start-->
				<view class="tr">
					<view class="td bg-g">建设地点</view>
					<view class="td">{{projectInfo.buildSite}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">建设年限</view>
					<view class="td">{{projectInfo.buildYear}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">新建/续建</view>
					<view class="td" wx:if="{{projectInfo.buildType==1}}">续建</view>
					<view class="td" wx:if="{{projectInfo.buildType==0}}">新建</view>
				</view>
				<view class="tr">
					<view class="td bg-g">是否完成征地拆迁</view>
					<view class="td" wx:if="{{projectInfo.isExpropriation==1}}">是</view>
					<view class="td" wx:if="{{projectInfo.isExpropriation==0}}">否</view>
				</view>
				<view class="tr">
					<view class="td bg-g">是否列入市级调度</view>
					<view class="td" wx:if="{{projectInfo.isDispatch==1}}">是</view>
					<view class="td" wx:if="{{projectInfo.isDispatch==0}}">否</view>
				</view>
				<view class="tr">
					<view class="td bg-g">挂点领导</view>
					<view class="td">{{projectInfo.leaderName}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">责任单位</view>
					<view class="td">{{projectInfo.responsibleUnits}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">招标方式</view>
					<view class="td" wx:if="{{projectInfo.biddingType==1}}">直接委托</view>
					<view class="td" wx:if="{{projectInfo.biddingType==0}}">公开招标</view>
				</view>
				<!--相同的stop-->
			</view>
		</block>
		<!--房建类stop-->
		<!--园林类start-->
		<block wx:if="{{categoryType == 2}}">
			<view class="table">
				<view class="tr bg-w">
					<view class="th jbxx" bindtap="toJbxx">基本信息</view>
				</view>

				<view class="table">
					<view class="tr">
						<view class="td bg-g">项目名称</view>
						<view class="td">{{projectInfo.projectName}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">占地面积</view>
						<view class="td">{{projectInfo.coverArea}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">总建筑面积</view>
						<view class="td">{{projectInfo.totalArea}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">园路面积</view>
						<view class="td">{{projectInfo.pathArea}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">铺装面积</view>
						<view class="td">{{projectInfo.paveArea}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">景观桥(座)</view>
						<view class="td">{{projectInfo.landscapeBridge}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">乔木数(颗)</view>
						<view class="td">{{projectInfo.treeNum}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">亮化数(盏)</view>
						<view class="td">{{projectInfo.brightenNum}}</view>
					</view>

					<!--相同的start-->
					<view class="tr">
						<view class="td bg-g">建设地点</view>
						<view class="td">{{projectInfo.buildSite}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">建设年限</view>
						<view class="td">{{projectInfo.buildYear}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">新建/续建</view>
						<view class="td" wx:if="{{projectInfo.buildType==1}}">续建</view>
						<view class="td" wx:if="{{projectInfo.buildType==0}}">新建</view>
					</view>
					<view class="tr">
						<view class="td bg-g">是否完成征地拆迁</view>
						<view class="td" wx:if="{{projectInfo.isExpropriation==1}}">是</view>
						<view class="td" wx:if="{{projectInfo.isExpropriation==0}}">否</view>
					</view>
					<view class="tr">
						<view class="td bg-g">是否列入市级调度</view>
						<view class="td" wx:if="{{projectInfo.isDispatch==1}}">是</view>
						<view class="td" wx:if="{{projectInfo.isDispatch==0}}">否</view>
					</view>
					<view class="tr">
						<view class="td bg-g">挂点领导</view>
						<view class="td">{{projectInfo.leaderName}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">责任单位</view>
						<view class="td">{{projectInfo.responsibleUnits}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">招标方式</view>
						<view class="td" wx:if="{{projectInfo.biddingType==1}}">直接委托</view>
						<view class="td" wx:if="{{projectInfo.biddingType==0}}">公开招标</view>
					</view>
					<!--相同的stop-->
				</view>
			</view>
		</block>
		<!--园林类stop-->
		<!--市政类start-->
		<block wx:if="{{categoryType == 3}}">
			<view class="table">
				<view class="tr bg-w">
					<view class="th jbxx" bindtap="toJbxx">基本信息</view>
				</view>
				<view class="table">
					<view class="tr">
						<view class="td bg-g">项目名称</view>
						<view class="td">{{projectInfo.projectName}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">占地面积</view>
						<view class="td">{{projectInfo.coverArea}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">总建筑面积</view>
						<view class="td">{{projectInfo.totalArea}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">道路长</view>
						<view class="td">{{projectInfo.roadLength}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">道路宽</view>
						<view class="td">{{projectInfo.roadWidth}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">行车道宽</view>
						<view class="td">{{projectInfo.carriagewayWidth}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">人行道宽</view>
						<view class="td">{{projectInfo.sidewalkWidth}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">花池宽</view>
						<view class="td">{{projectInfo.pondWidth}}</view>
					</view>

					<!--相同的start-->
					<view class="tr">
						<view class="td bg-g">建设地点</view>
						<view class="td">{{projectInfo.buildSite}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">建设年限</view>
						<view class="td">{{projectInfo.buildYear}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">新建/续建</view>
						<view class="td" wx:if="{{projectInfo.buildType==1}}">续建</view>
						<view class="td" wx:if="{{projectInfo.buildType==0}}">新建</view>
					</view>
					<view class="tr">
						<view class="td bg-g">是否完成征地拆迁</view>
						<view class="td" wx:if="{{projectInfo.isExpropriation==1}}">是</view>
						<view class="td" wx:if="{{projectInfo.isExpropriation==0}}">否</view>
					</view>
					<view class="tr">
						<view class="td bg-g">是否列入市级调度</view>
						<view class="td" wx:if="{{projectInfo.isDispatch==1}}">是</view>
						<view class="td" wx:if="{{projectInfo.isDispatch==0}}">否</view>
					</view>
					<view class="tr">
						<view class="td bg-g">挂点领导</view>
						<view class="td">{{projectInfo.leaderName}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">责任单位</view>
						<view class="td">{{projectInfo.responsibleUnits}}</view>
					</view>
					<view class="tr">
						<view class="td bg-g">招标方式</view>
						<view class="td" wx:if="{{projectInfo.biddingType==1}}">直接委托</view>
						<view class="td" wx:if="{{projectInfo.biddingType==0}}">公开招标</view>
					</view>
					<!--相同的stop-->
				</view>
			</view>
		</block>
		<!--市政类stop-->
		<!-- ==================基本信息end================== -->


		<!-- =======================五方信息start=========================-->
		<view class="table">
			<view class="tr bg-w">
				<view class="th"  id="wfxx" bindtap="towfxx">五方信息</view>
			</view>

			<view class="tr">
				<view class="td bg-g">项目业主</view>
				<view class="td">{{projectInfo.ownerCompanyName}}</view>
			</view>
			<view class="tr">
				<view class="td bg-g">项目负责人</view>
				<view class="td">{{projectInfo.ownerProjectLeader}}</view>
			</view>
			<view class="tr">
				<view class="td bg-g">电话</view>
				<view class="td">{{projectInfo.ownerPhone}}</view>
			</view>
			<view class="tr">
				<view class="td bg-g">施工单位</view>
				<view class="td">{{projectInfo.constructionCompanyName}}</view>
			</view>
			<view class="tr">
				<view class="td bg-g">项目负责人</view>
				<view class="td">{{projectInfo.constructionProjectLeader}}</view>
			</view>
			<view class="tr">
				<view class="td bg-g">电话</view>
				<view class="td">{{projectInfo.constructionPhone}}</view>
			</view>
			<view class="tr">
				<view class="td bg-g">监理单位</view>
				<view class="td">{{projectInfo.supervisionCompanyName}}</view>
			</view>
			<view class="tr">
				<view class="td bg-g">总监</view>
				<view class="td">{{projectInfo.supervisionDirectorName}}</view>
			</view>
			<view class="tr">
				<view class="td bg-g">电话</view>
				<view class="td">{{projectInfo.supervisionPhone}}</view>
			</view>
			<view class="tr">
				<view class="td bg-g">设计单位</view>
				<view class="td">{{projectInfo.designCompanyName}}</view>
			</view>
			<view class="tr">
				<view class="td bg-g">项目负责人</view>
				<view class="td">{{projectInfo.designProjectLeader}}</view>
			</view>
			<view class="tr">
				<view class="td bg-g">电话</view>
				<view class="td">{{projectInfo.designPhone}}</view>
			</view>
			<view class="tr">
				<view class="td bg-g">勘察单位</view>
				<view class="td">{{projectInfo.surveyCompanyName}}</view>
			</view>
			<view class="tr">
				<view class="td bg-g">项目负责人</view>
				<view class="td">{{projectInfo.surveyProjectLeader}}</view>
			</view>
			<view class="tr">
				<view class="td bg-g">电话</view>
				<view class="td">{{projectInfo.surveyPhone}}</view>
			</view>

		</view>
		<!-- ==================五方信息stop===============================-->


		<!-- 项目资金情况与单体情况 -->
		<!-- 房建类项目-start -->
		<block wx:if="{{categoryType == 1}}">
			<view class="table">
				<view class="tr bg-w">
					<view class="th zjqk" bindtap="toZjqk">项目资金情况</view>
				</view>
				<view wx:for="{{projectInfo.investments}}" wx:key="index" wx:for-item="item">
					<view class="tr" wx:if="{{item.investmentType == 0}}">
						<view class="td bg-g">项目总投资</view>
						<view class="td">{{item.totalMoney}}</view>
					</view>
					<view class="tr" wx:if="{{item.investmentType == 1}}">
						<view class="td bg-g">本年投资</view>
						<view class="td">{{item.totalMoney}}</view>
					</view>
					<view class="tr" wx:if="{{item.investmentType == 2}}">
						<view class="td bg-g">第二年年度投资</view>
						<view class="td">{{item.totalMoney}}</view>
					</view>
					<view class="tr" wx:if="{{item.investmentType == 3}}">
						<view class="td bg-g">第三年年度投资</view>
						<view class="td">{{item.totalMoney}}</view>
					</view>
				</view>
				<view class="tr">
					<view class="td bg-g">工程建安费</view>
					<view class="td">{{projectInfo.jiananCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">房建造价</view>
					<view class="td">{{projectInfo.houseBuildCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">配套造价</view>
					<view class="td">{{projectInfo.supportCost}}</view>
				</view>

				<view class="tr">
					<view class="td bg-g">供水造价</view>
					<view class="td">{{projectInfo.waterCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">供电造价</view>
					<view class="td">{{projectInfo.electricityCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">供气造价</view>
					<view class="td">{{projectInfo.airCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">设计费用</view>
					<view class="td">{{projectInfo.designCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">勘察费用</view>
					<view class="td">{{projectInfo.surveyCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">检测费用</view>
					<view class="td">{{projectInfo.checkCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">监理费用</view>
					<view class="td">{{projectInfo.supervisionCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">图审费用</view>
					<view class="td">{{projectInfo.drawCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">其它费用</view>
					<view class="td">{{projectInfo.otherCost}}</view>
				</view>
			</view>
			<view class="table">
					<view class="tr bg-w">
						<view class="th">单体情况</view>
					</view>
					<view class="tr bg-g">
						<view class="td">楼栋号</view>
						<view class="td">工程造价</view>
						<view class="td">建筑面积</view>
						<view class="td">层数</view>
					</view>
					<view wx:for="{{projectInfo.zhuti}}" wx:key="index" wx:for-item="item">
						<view class="tr">
							<view class="td">{{item.ztName}}</view>
							<view class="td">{{item.projectCost}}</view>
							<view class="td">{{item.buildArea}}</view>
							<view class="td">{{item.floors}}</view>
						</view>
					</view>
			</view>
		</block>
		<!-- 房建类项目-end -->
		<!--园林类start-->
		<block wx:if="{{categoryType == 2}}">
			<view class="table">
				<view class="tr bg-w">
					<view class="th zjqk" bindtap="toZjqk">项目资金情况</view>
				</view>
				<view wx:for="{{projectInfo.investments}}" wx:key="index" wx:for-item="item">
					<view class="tr" wx:if="{{item.investmentType == 0}}">
						<view class="td bg-g">项目总投资</view>
						<view class="td">{{item.totalMoney}}</view>
					</view>
					<view class="tr" wx:if="{{item.investmentType == 1}}">
						<view class="td bg-g">本年投资</view>
						<view class="td">{{item.totalMoney}}</view>
					</view>
					<view class="tr" wx:if="{{item.investmentType == 2}}">
						<view class="td bg-g">第二年年度投资</view>
						<view class="td">{{item.totalMoney}}</view>
					</view>
					<view class="tr" wx:if="{{item.investmentType == 3}}">
						<view class="td bg-g">第三年年度投资</view>
						<view class="td">{{item.totalMoney}}</view>
					</view>
				</view>
				<view class="tr">
					<view class="td bg-g">工程建安费</view>
					<view class="td">{{projectInfo.jiananCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">园林造价</view>
					<view class="td">{{projectInfo.parkdCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">配套用房造价</view>
					<view class="td">{{projectInfo.supportHouseCost}}</view>
				</view>

				<view class="tr">
					<view class="td bg-g">路场造价</view>
					<view class="td">{{projectInfo.roadCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">文化景观造价</view>
					<view class="td">{{projectInfo.culturalCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">设计费用</view>
					<view class="td">{{projectInfo.designCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">勘察费用</view>
					<view class="td">{{projectInfo.surveyCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">检测费用</view>
					<view class="td">{{projectInfo.checkCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">监理费用</view>
					<view class="td">{{projectInfo.supervisionCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">图审费用</view>
					<view class="td">{{projectInfo.drawCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">其它费用</view>
					<view class="td">{{projectInfo.otherCost}}</view>
				</view>
			</view>
		</block>
		<!--园林类 end-->
		<!--市政类start-->
		<block wx:if="{{categoryType == 3}}">
			<view class="table">
				<view class="tr bg-w">
					<view class="th zjqk" bindtap="toZjqk">项目资金情况</view>
				</view>
				<view wx:for="{{projectInfo.investments}}" wx:key="index" wx:for-item="item">
					<view class="tr" wx:if="{{item.investmentType == 0}}">
						<view class="td bg-g">项目总投资</view>
						<view class="td">{{item.totalMoney}}</view>
					</view>
					<view class="tr" wx:if="{{item.investmentType == 1}}">
						<view class="td bg-g">本年投资</view>
						<view class="td">{{item.totalMoney}}</view>
					</view>
					<view class="tr" wx:if="{{item.investmentType == 2}}">
						<view class="td bg-g">第二年年度投资</view>
						<view class="td">{{item.totalMoney}}</view>
					</view>
					<view class="tr" wx:if="{{item.investmentType == 3}}">
						<view class="td bg-g">第三年年度投资</view>
						<view class="td">{{item.totalMoney}}</view>
					</view>
				</view>
				<view class="tr">
					<view class="td bg-g">工程建安费</view>
					<view class="td">{{projectInfo.jiananCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">市政造价</view>
					<view class="td">{{projectInfo.municipalCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">桥梁造价</view>
					<view class="td">{{projectInfo.bridgeCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">给排水造价</view>
					<view class="td">{{projectInfo.drainageCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">交通工程造价</view>
					<view class="td">{{projectInfo.trafficCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">设计费用</view>
					<view class="td">{{projectInfo.designCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">勘察费用</view>
					<view class="td">{{projectInfo.surveyCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">检测费用</view>
					<view class="td">{{projectInfo.checkCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">监理费用</view>
					<view class="td">{{projectInfo.supervisionCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">图审费用</view>
					<view class="td">{{projectInfo.drawCost}}</view>
				</view>
				<view class="tr">
					<view class="td bg-g">其它费用</view>
					<view class="td">{{projectInfo.otherCost}}</view>
				</view>
			</view>
		</block>
		<!--市政类 end-->
	</block>
</view>